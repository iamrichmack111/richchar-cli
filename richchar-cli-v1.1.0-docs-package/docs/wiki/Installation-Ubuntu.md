# Installation — Ubuntu

Install Python, Git, FFmpeg/ffprobe and the dependencies required by Piper, Wav2Lip and GFPGAN. Configure Linux-specific runtime/model paths and run `./richchar --doctor` before rendering. Do not copy macOS paths verbatim.
