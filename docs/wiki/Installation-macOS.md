# Installation — macOS

Richchar expects local Piper, Wav2Lip, GFPGAN, FFmpeg/ffprobe and compatible Python environments. Make scripts executable and run `./richchar --doctor` before the first render. Apple Silicon is the primary validated development platform.
