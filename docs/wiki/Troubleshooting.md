# Troubleshooting

Start with `./richchar --doctor`. For face-detection failures on suitable photographic portraits, use a manual `--box TOP BOTTOM LEFT RIGHT`. Blurry output should be evaluated against the exact GFPGAN quality baseline rather than hidden with aggressive sharpening or ROI-only shortcuts.
